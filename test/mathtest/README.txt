mathtest
========

This set of tests validates the math library functionality against itself.

Dependencies
============

To build the binary, the following dependencies must exist in the specified
directories relative to this document.

    taamath  - ../../
    taasdk   - ../../../taasdk


