vputest
=======

This set of tests validates the vector processing unit macro implementations
against the fallback floating point unit macros.

Dependencies
============

To build the binary, the following dependencies must exist in the specified
directories relative to this document.

    taamath  - ../../
    taasdk   - ../../../taasdk



