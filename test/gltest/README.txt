gltest
======

This set of tests validates the math library functionality against the native
OpenGL implementation of the target platform.

Dependencies
============

To build the binary, the following dependencies must exist in the specified
directories relative to this document.

    taamath  ../../
    taasdk   ../../../taasdk
    GL
    GLU
    X11

