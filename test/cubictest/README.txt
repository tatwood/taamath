cubictest
=========

This test validates cubic root solver.

Dependencies
============

To build the binary, the following dependencies must exist in the specified
directories relative to this document.

    taamath  ../../
    taasdk   ../../../taasdk

